# Fashion-MNIST
A PyTorch-based neural network trained on the Fashion-MNIST dataset, covering the full deep learning pipeline from data preprocessing and training to evaluation and image inference. The project focuses on understanding model behavior, regularization, and end-to-end deployment basics.
